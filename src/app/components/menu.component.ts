import { templateJitUrl } from '@angular/compiler';
import { Component } from '@angular/core';

@Component({
  selector: 'menu-component',
  template: '<h2>Nonato</h2>',
  styles: ['h2{color: blue}'],
})
export class MenuComponent {}
